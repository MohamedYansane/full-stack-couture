   <!-- <div class="table-content rounded-md shadow-lg">
        <table class=" ">
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>Prenom</th>
                    <th>Phone</th>
                    <th>Adresse</th>
                    <th>Email</th>
                    <th>Cni</th>
                    <th>Sexe</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <tr *ngFor="let user of clients">
                    <td>{{ user.nom }}</td>
                    <td>{{ user.prenom }}</td>
                    <td>{{ user.phone }}</td>
                    <td>{{ user.adresse }}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.cni }}</td>
                    <td>{{ user.sexe }}</td>
                    !-- second method  [ngClass]="user.status" --
                    <td [ngClass]="[user.status, 'status']">{{ user.status }}</td>
                    <td>
                        <span class="flex gap-x-2 items-center">
                            <fa-icon
                                class="cursor-pointer"
                                [icon]="faEdit"
                                (click)="editUser(user.id)"
                            ></fa-icon>
                            <fa-icon
                                class="cursor-pointer"
                                [icon]="faTrash"
                                (click)="deleteUser(user.id)"
                            ></fa-icon>
                            <fa-icon
                                class="cursor-pointer"
                                [icon]="faEye"
                                (click)="deleteUser(user.id)"
                            ></fa-icon>
                        </span>
                    </td>
                </tr>
            </tbody>
        </table>
    </div> -->
